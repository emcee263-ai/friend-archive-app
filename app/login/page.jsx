"use client";import {useState} from "react";import {useRouter} from "next/navigation";export default function Login(){const router=useRouter(),[email,setEmail]=useState(""),[password,setPassword]=useState("");return <main className="auth-page"><div className="auth-card"><div className="brand">THE ARCHIVE</div><p className="hero-label">WELCOME BACK</p><h1>Back to<br/><em>your people.</em></h1><form onSubmit={e=>{e.preventDefault();router.push("/dashboard")}}><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><label>Password<input type="password" required value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••"/></label><button className="create-submit">Enter the Archive <span>→</span></button></form><p className="auth-foot">New here? <button onClick={()=>router.push("/signup")}>Create an account</button></p></div></main>}