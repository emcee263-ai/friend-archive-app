"use client";import {useState} from "react";import {useRouter} from "next/navigation";export default function Signup(){const router=useRouter(),[name,setName]=useState(""),[email,setEmail]=useState(""),[password,setPassword]=useState("");return <main className="auth-page"><div className="auth-card"><div className="brand">THE ARCHIVE</div><p className="hero-label">MAKE IT YOURS</p><h1>Create your<br/><em>place.</em></h1><form onSubmit={e=>{e.preventDefault();router.push("/create-space")}}><label>Your name<input required value={name} onChange={e=>setName(e.target.value)} placeholder="Alex"/></label><label>Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><label>Password<input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="8+ characters"/></label><button className="create-submit">Continue <span>→</span></button></form><p className="auth-foot">Already have an account? <button onClick={()=>router.push("/login")}>Sign in</button></p></div></main>}