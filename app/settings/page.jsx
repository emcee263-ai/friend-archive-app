"use client";import {useEffect,useState} from "react";import {Shell} from "../../lib/ui";import {loadDB,resetDB} from "../../lib/store";export default function Settings(){const[db,setDb]=useState(null);useEffect(()=>setDb(loadDB()),[]);if(!db)return null;return <Shell><div className="page-top"><div><p className="hero-label">SETTINGS</p><h1>Your Archive.</h1><p className="muted">Mock account settings for the test build.</p></div></div><section className="settings-card"><label>Name<input value={db.user.name} readOnly/></label><label>Username<input value={'@'+db.user.username} readOnly/></label><label>Email<input value={db.user.email} readOnly/></label><div className="setting-row"><div><strong>Private by design</strong><p>In this prototype, data is stored as JSON in your browser’s local storage.</p></div><span>✓</span></div><button className="danger-button" onClick={resetDB}>Reset demo data</button></section></Shell>}
