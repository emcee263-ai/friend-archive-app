"use client";import {useEffect,useState} from "react";import {Shell} from "../../lib/ui";import {loadDB} from "../../lib/store";export default function Achievements(){const[db,setDb]=useState(null);useEffect(()=>setDb(loadDB()),[]);if(!db)return null;return <Shell><div className="page-top"><div><p className="hero-label">ACHIEVEMENTS</p><h1>Keep the streak<br/><em>alive.</em></h1><p className="muted">Little milestones for a friendship with history.</p></div></div><div className="achievement-grid">{db.achievements.map(a=><article className={'achievement '+(a.done?'unlocked':'')} key={a.id}><div>{a.icon}</div><small>{a.done?'UNLOCKED':'LOCKED'}</small><h3>{a.title}</h3><p>{a.description}</p></article>)}</div></Shell>}
