"use client";import {useState} from "react";import {useRouter} from "next/navigation";export default function Join(){const[code,setCode]=useState(""),router=useRouter();return <main className="auth-page"><div className="auth-card"><button className="back-button" onClick={()=>router.push("/")}>←</button><div className="brand">THE ARCHIVE</div><p className="hero-label">YOU'VE BEEN INVITED</p><h1>Come on<br/><em>in.</em></h1><p>Enter the invite code your friend shared with you.</p><form onSubmit={e=>{e.preventDefault();if(code.trim())router.push("/dashboard")}}><label>Invite code<input value={code} onChange={e=>setCode(e.target.value)} placeholder="ARCHIVE-1234"/></label><button className="create-submit">Join the space <span>→</span></button></form></div></main>}